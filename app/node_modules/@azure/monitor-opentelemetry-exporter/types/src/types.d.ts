import { ExportResult } from "@opentelemetry/core";
import { ContextTagKeys, TelemetryItem as Envelope } from "./generated";
export declare type Tags = {
    [key in ContextTagKeys]: string;
};
export declare type PropertyType = string | number | boolean | object | Array<PropertyType>;
export declare type Properties = {
    [key: string]: Properties | PropertyType;
};
export interface MSLink {
    operation_Id: string;
    id: string;
}
export declare type Measurements = {
    [key: string]: number;
};
export declare type TelemetryProcessor = (envelope: Envelope) => boolean | void;
export declare type SenderResult = {
    statusCode: number;
    result: string;
};
export interface BaseExporter {
    addTelemetryProcessor(processor: TelemetryProcessor): void;
    clearTelemetryProcessors(): void;
    exportEnvelopes(envelopes: Envelope[]): Promise<ExportResult>;
}
export interface Sender {
    send(payload: unknown[]): Promise<SenderResult>;
    shutdown(): void;
}
export interface PersistentStorage {
    shift(): Promise<unknown>;
    push(value: unknown[]): Promise<boolean>;
}
//# sourceMappingURL=types.d.ts.map